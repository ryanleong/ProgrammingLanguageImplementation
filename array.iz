proc main ()

int totalDimensions;
int dimension;
int offset;
int hid;
int lod;
int id;


while dimension <= 3 do
	
	offset := offset * (5 - 1 + 1);

	offset := offset + id - 1;

	dimension := dimension + 1;
od

write offset;

end
